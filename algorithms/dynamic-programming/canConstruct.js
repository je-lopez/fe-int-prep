// given a targetString and array of strings, return whether or not targetStr can be constructed by concatenating strings in given array
//   strings in array may be reused
//
// m = targetStr.length
// n = stringsArr.length

const canConstruct1 = (targetStr, stringArr) => {

};